export default (state = [], action) => {
    switch (action.type) {
        case 'SHOW_RANDOM_FIB':
            // This would be logic to change visible fib
            return state;
        default:
            return state;
    }
};